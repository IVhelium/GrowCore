VITE_API_URL=/api
VITE_API_PROXY_TARGET=http://localhost:8000

# Required in production when /api is served through a proxy that does not
# support WebSocket upgrades. Configure the real value in Vercel, not Git.
VITE_WS_URL=wss://your-backend.example.com
